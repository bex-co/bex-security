---
id: w1/m1/t007
title: Publish the fixed Muse ACP package and repeat the installed-package verification
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m1/t006]
---

## Objective

Publish the verified Muse ACP adapter, pin Bex to the released version, install from the registry in a clean workspace, and rerun the same private end-to-end verification loop.

## Context

- Publishing happens only after the packed-package loop passes, avoiding intermediate broken releases.
- Registry credentials are maintainer-provided execution state and must never be printed or committed.
- Registry packaging can differ from the local workspace, so the full verification must be repeated after installation.

## Steps

1. Inspect the package tarball and published file list for required runtime files and public-safe metadata.
2. Publish the next verified `@bex-co/muse-code-acp` version using maintainer-provided npm authentication.
3. Pin the exact released version in Bex and regenerate the lockfile.
4. Install cleanly from the registry and repeat t006's full scan and evidence gates.
5. If the installed-package run fails, add a regression, fix the owning layer, publish a corrected version, and rerun until it passes.

## Files

- `.internal/muse-code-acp/package.json`
- `.internal/muse-code-acp/README.md`
- `sdk/typescript/package.json`
- `sdk/typescript/pnpm-lock.yaml`

## Acceptance criteria

- [x] The npm package installs without workspace-only files or unpublished dependencies.
- [x] Bex pins the exact verified release.
- [x] The registry-installed package passes the same private full-scan gates as the packed artifact.
- [x] Published metadata, logs, and notes contain no credentials, target identity, or findings.

## Verification

- Published and registry-installed `@bex-co/muse-code-acp@0.1.1`; the resolved dependency is the registry package, not a workspace or file reference.
- Added a generic regression for adjacent strict JSON objects aggregated into one ACP agent message after the installed-package run exposed that concrete boundary.
- The repeated full verification completed with zero process failure after 118 minutes, reviewed all 9,332 authoritative inventory files, produced complete coverage across 12 surfaces, and left no deferred surface work.
- Public package metadata and board evidence were reviewed without recording credentials, target identity, repository paths, or findings.

## Out of scope

- Publishing Bex Security itself.

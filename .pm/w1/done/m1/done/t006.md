---
id: w1/m1/t006
title: Run the private adjacent-repository Muse verification loop
worker: worker1
status: done
estimate: 60m
depends_on: [w1/m1/t005]
---

## Objective

Run the existing Muse scan command against the maintainer-provided adjacent repository using the locally packed adapter, and repeat diagnose, regression-test, fix, and full verification until every acceptance gate passes.

## Context

- The exact local target and any findings are execution-only data and must not be committed to this public board, logs, fixtures, commits, or pull requests.
- Use the existing public command shape with agent `muse`, model `muse-spark-1.2-contributor`, and effort `high`; do not add a verification-only CLI flag.
- A fast scan is acceptable only when host evidence proves the full authoritative inventory was reviewed; elapsed time alone is not a success criterion.

## Steps

1. Build Bex and install the locally packed Muse ACP artifact.
2. Run the requested full repository scan on the maintainer-provided adjacent target.
3. Verify workspace isolation, no approval wait, monotonic evidence-backed progress, assignment retries, valid canonical artifacts, and consistent exclusions/findings.
4. If any gate fails, identify the owning layer, add a regression test, fix it there, and repeat from step 1.
5. Keep this task open until one uninterrupted full run passes every gate; retain only sanitized aggregate verification notes.

## Files

- `.internal/muse-code-acp/`
- `sdk/typescript/`
- Local scan state outside the repository (execution only)

## Acceptance criteria

- [x] The requested Muse command exits zero without an approval deadlock.
- [x] Every completed file count is backed by accepted review evidence for the authoritative inventory.
- [x] Canonical artifacts validate and coverage is complete only when all assignments and candidate validations finish.
- [x] Findings never reference excluded paths.
- [x] Every failure encountered during the loop has a regression test before the final rerun.
- [x] No target identity, source, findings, credentials, or environment details are committed or published.

## Verification

- A locally packed adapter completed one uninterrupted full scan in 100m 58s with 9,332/9,332 evidence-backed files and a zero exit status.
- The audit artifact recorded 232 assignments, including 85 retried assignments and 27 assignments that retained early-round gaps; the final missing-file count was zero.
- Canonical coverage was complete across 12 surfaces with no deferred work or open questions.
- Regression coverage was added for every diagnosed failure before the passing rerun.

## Out of scope

- Comparing finding counts as a proxy for scan quality.

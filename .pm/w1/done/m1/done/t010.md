---
id: w1/m1/t010
title: Run CI and complete behavioral test coverage
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m1/t008, w1/m1/t009]
---

## Objective

Run `/ci` for both affected repositories and ensure meaningful tests cover the shipped success and failure behavior before closeout.

## Context

- Required coverage includes adapter conformance, approval non-hanging behavior, workspace isolation, evidence-backed progress, fallback retries, package installation, and terminal completion.
- Tests must exercise real behavior rather than snapshots or tautological coverage.

## Steps

1. Run formatting, lint/type checks, unit tests, integration tests, package smoke tests, and production dependency audits for affected packages.
2. Run repository CI checks through `/ci` and fix every in-scope failure.
3. Confirm private end-to-end verification evidence remains passing after final code changes.

## Files

- `.internal/muse-code-acp/`
- `sdk/typescript/`
- CI configuration used by both repositories

## Acceptance criteria

- [x] All affected CI checks pass from clean installs with the required Python runtime.
- [x] Behavioral tests cover every failure reproduced in t001, t006, and t007.
- [x] Package smoke tests use the exact registry-pinned Muse ACP release.

## Verification

- Parent CI passed frozen install, formatting, type checking, production build, 1,889 tests across 97 files, and installed-package smoke from a clean npm cache.
- The container package stage passed with the workspace supply-chain policy loaded before the frozen install.
- Package smoke validated 282 tar entries, public imports and types, both CLI launchers, 113 bundled plugin files, and a nested worker.
- The exact registry dependency `@bex-co/muse-code-acp@0.1.1` installed in the package smoke consumer.
- Adapter CI passed clean install, production audit with zero vulnerabilities, lint, formatting, build, 97 tests, and a 62-file package dry run.
- The only parent audit advisory has no published fixed package version and is ignored by exact GHSA because the existing extractor rejects every symlink ZIP entry before creation; its focused unsafe/duplicate/symlink ZIP test passed.

## Out of scope

- Fixing unrelated pre-existing CI failures without documenting why they block this milestone.
